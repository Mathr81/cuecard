{$CUECARD_DOMAIN} {
	encode zstd gzip

	# cuecard n'a aucune authentification : sur un domaine public, n'importe
	# qui peut brûler tes quotas OpenSubtitles et OpenRouter. Pour le fermer :
	#
	#   1. docker run --rm caddy:2-alpine caddy hash-password --plaintext 'ton-mot-de-passe'
	#   2. dans .env : CUECARD_BASIC_AUTH="moi <le-hash-obtenu>"
	#   3. décommente les trois lignes ci-dessous
	#   4. docker compose --profile https restart caddy
	#
	# basic_auth {
	# 	{$CUECARD_BASIC_AUTH}
	# }

	reverse_proxy app:3000
}
